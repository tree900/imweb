/*! THREE.Font - minimal version */
THREE.Font = function (data) {
  this.data = data;
  this.generateShapes = function (text, size) {
    const shapes = [];
    for (let i = 0; i < text.length; i++) {
      shapes.push({ text: text[i], size: size });
    }
    return shapes;
  };
};
